// Defining a functional component named Create
const Create = () => {
    return (
        <div>
            {/* Displaying a header message for the Create component */}
            <h1>Hello from Create Component</h1>
        </div>
    );
}

// Exporting the Create component so it can be imported and used in other parts of the application
export default Create;
