// Define a functional component called Header
// (React components should always start with a capital letter)
const Header = () => {

    return (
        <div>
            {/* Header heading displayed on the page */}
            <h1>My Header in another component</h1>
        </div>
    );
}

// Export Header so it can be imported and used in other files (like App.js)
export default Header;
