// Define a functional component called Footer
const Footer = () => {

    return (
        <div>
            {/* Footer heading displayed on the page */}
            <h1>My footer in another component</h1>
        </div>
    );
}

// Export Footer so it can be imported and used in other files (like App.js)
export default Footer;
